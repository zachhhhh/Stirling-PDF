# SaaS mode is disabled by default. Override these properties in your deployment-specific
# settings.yml or environment variables to enable multi-tenant SaaS behaviour.
saas.enabled=false
saas.default-tenant-slug=default
saas.tenant-header=X-Tenant-Slug
# Optional: domain suffix used to resolve tenants from subdomains (e.g. tenant.example.com)
# saas.domain=example.com
# saas.default-plan=FREE
# saas.default-monthly-operation-limit=1000
# saas.default-storage-limit-mb=2048
# saas.trial-period=PT336H
